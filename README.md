
deactivate
python -m venv myvenv

.venv\Scripts\activate.ps1

python -m venv my_project_environment
my_project_environment\Scripts\activate.ps1

pip install aiogram
pip list
pip install requests
poetry
pip install requests==2.26
pip freeze
python -m venv newvenv
.venv\Scripts\activate.ps1

python -m venv .venv
python3 -m venv .venv


twovenv\Scripts\activate.ps1

pip install -r requirements.txt
poetry remove requests
poetry new new_project
poetry init
poetry install
(Invoke-WebRequest -Uri https://install.python-poetry.org -UseBasicParsing).Content | py -
python -m pip install pipx
pipx ensurepath
pipx install poetry
pipx upgrade poetry
poetry --version

Set-ExecutionPolicy -Scope Aleksandr.Orlov AllSigned
Set-ExecutionPolicy -Scope CurrentUser AllSigned

git --version
git init
git config --global user.name "Alex"
git config --list
git commit -m "Add README"
git log
git branch
git add .
git commit -m "All project"
git branch -M main
git remote add origin https://github.com/AlexOrlov222/assistent_bot
git push -u origin main

git clone https://github.com/AlexOrlov222/assistent_bot.git
